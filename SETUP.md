# 🛠️ Інструкція по налаштуванню

Детальна покрокова інструкція для налаштування Instagram SMM Analytics System.

## Передумови

### Системні вимоги
- Python 3.10 або новіше
- pip (менеджер пакетів Python)
- Git
- 500 MB вільного місця на диску
- Інтернет з'єднання

### Необхідні облікові записи
1. **Instagram Business Account** або Creator Account
2. **Telegram Bot** (створюється через @BotFather)
3. **OpenAI Account** з API ключем

---

## Крок 1: Клонування репозиторію

```bash
git clone https://github.com/kobzarbro-droid/smm-analise.git
cd smm-analise
```

---

## Крок 2: Створення віртуального середовища (рекомендовано)

### Windows:
```bash
python -m venv venv
venv\Scripts\activate
```

### Linux/Mac:
```bash
python3 -m venv venv
source venv/bin/activate
```

---

## Крок 3: Встановлення залежностей

```bash
pip install -r requirements.txt
```

Це встановить всі необхідні бібліотеки:
- instagrapi (Instagram API)
- python-telegram-bot (Telegram)
- openai (AI аналіз)
- flask (Web dashboard)
- sqlalchemy (База даних)
- та інші...

---

## Крок 4: Налаштування Instagram

### 4.1 Перевірте тип акаунту
Instagram API працює найкраще з Business або Creator акаунтами.

### 4.2 Вимкніть двофакторну автентифікацію (тимчасово)
Для автоматизації рекомендується тимчасово вимкнути 2FA, або використовувати окремий акаунт.

### 4.3 Запам'ятайте credentials
- Username (без @)
- Password

---

## Крок 5: Створення Telegram Bot

### 5.1 Відкрийте Telegram та знайдіть @BotFather

### 5.2 Створіть нового бота
```
/newbot
```

### 5.3 Дайте назву боту
```
SMM Analytics Bot
```

### 5.4 Дайте username боту
```
my_smm_analytics_bot
```

### 5.5 Збережіть токен
BotFather надасть токен у форматі: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`

### 5.6 Отримайте Chat ID
1. Відправте повідомлення вашому боту
2. Відкрийте у браузері:
```
https://api.telegram.org/bot<YOUR_TOKEN>/getUpdates
```
3. Знайдіть `"chat":{"id":123456789}`
4. Це ваш Chat ID

---

## Крок 6: Отримання OpenAI API ключа

### 6.1 Зареєструйтесь на https://platform.openai.com

### 6.2 Перейдіть у API Keys
https://platform.openai.com/api-keys

### 6.3 Створіть новий ключ
- Натисніть "Create new secret key"
- Дайте назву: "SMM Analytics"
- Збережіть ключ (він показується тільки один раз!)

### 6.4 Поповніть баланс
Додайте кредити для використання API (мінімум $5).

---

## Крок 7: Конфігурація системи

### 7.1 Створіть конфігураційний файл
```bash
cp config/.env.example config/.env
```

### 7.2 Відредагуйте config/.env
```bash
# Linux/Mac
nano config/.env

# Windows
notepad config/.env
```

### 7.3 Заповніть всі змінні

**Instagram:**
```env
INSTAGRAM_USERNAME=your_instagram_username
INSTAGRAM_PASSWORD=your_instagram_password
```

**Telegram:**
```env
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_CHAT_ID=123456789
```

**OpenAI:**
```env
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxxx
OPENAI_MODEL=gpt-4
```

**Інші налаштування (за замовчуванням працюють добре):**
```env
DATABASE_URL=sqlite:///data/smm_analise.db
APP_ENV=production
DEBUG=False
LOG_LEVEL=INFO
DASHBOARD_HOST=0.0.0.0
DASHBOARD_PORT=5000
TIMEZONE=Europe/Kyiv
```

### 7.4 Збережіть файл

---

## Крок 8: Налаштування цілей та планів

Відредагуйте `config/targets.yaml`:

```yaml
# Акаунт для моніторингу
instagram_account: "your_instagram_username"

# Цілі
targets:
  posts_per_month: 20        # Постів на місяць
  stories_per_day: 3         # Stories на день
  reels_per_week: 2          # Reels на тиждень
  min_caption_length: 50     # Мінімальна довжина опису
  hashtags_per_post: 10      # Хештегів на пост
  min_engagement_rate: 3.5   # Мінімальний engagement %

# Конкуренти (опціонально)
competitors:
  - "competitor1_username"
  - "competitor2_username"
  - "competitor3_username"

# Налаштування якості контенту
content_quality:
  min_likes_threshold: 50
  min_comments_threshold: 5
  min_saves_threshold: 10

# Алерти
alerts:
  low_engagement_threshold: 2.0
  missed_daily_story_alert: True
  missed_weekly_reel_alert: True

# AI аналіз
ai_analysis:
  enabled: True
  analyze_captions: True
  analyze_hashtags: True
  generate_recommendations: True
  language: "uk"
```

---

## Крок 9: Ініціалізація бази даних

```bash
python -c "from src.database.models import init_db; init_db()"
```

Це створить файл `data/smm_analise.db` з усіма необхідними таблицями.

---

## Крок 10: Перевірка конфігурації

```bash
python -c "from config.settings import settings; errors = settings.validate(); print('OK' if not errors else errors)"
```

Якщо все налаштовано правильно, ви побачите `OK`.

---

## Крок 11: Перший запуск

### 11.1 Запустіть основний моніторинг
```bash
python main.py
```

Перший запуск може зайняти кілька хвилин:
- Авторизація в Instagram
- Збір початкових даних
- Налаштування scheduler

### 11.2 У новому терміналі запустіть Dashboard
```bash
python run_dashboard.py
```

### 11.3 Відкрийте браузер
```
http://localhost:5000
```

---

## Крок 12: Перевірка роботи

### 12.1 Перевірте логи
```bash
# Linux/Mac
tail -f logs/*.log

# Windows
type logs\*.log
```

### 12.2 Перевірте Dashboard
- Відкрийте http://localhost:5000
- Перевірте чи відображаються дані
- Перегляньте пости, аналітику

### 12.3 Перевірте Telegram
- Відправте `/start` вашому боту
- Спробуйте команду `/stats`
- Дочекайтесь першого автоматичного звіту (20:00)

---

## Docker запуск (альтернатива)

### Крок 1: Підготовка
```bash
# Створіть config/.env файл (як описано вище)
cp config/.env.example config/.env
# Відредагуйте config/.env
```

### Крок 2: Збірка та запуск
```bash
docker-compose up -d
```

### Крок 3: Перевірка
```bash
# Перегляд логів
docker-compose logs -f

# Перевірка статусу
docker-compose ps
```

### Крок 4: Зупинка
```bash
docker-compose down
```

---

## Розв'язання проблем

### Проблема: Instagram не авторизується
**Рішення:**
- Перевірте username та password
- Вимкніть 2FA
- Спробуйте увійти через браузер (може потрібна капча)
- Зачекайте 15 хвилин після невдалої спроби

### Проблема: Telegram бот не відповідає
**Рішення:**
- Перевірте токен бота
- Перевірте Chat ID
- Відправте `/start` боту
- Перевірте чи запущений main.py

### Проблема: OpenAI API помилки
**Рішення:**
- Перевірте API ключ
- Переконайтесь що є кредити на балансі
- Перевірте модель (gpt-4 або gpt-3.5-turbo)

### Проблема: Dashboard не відкривається
**Рішення:**
- Перевірте чи запущений run_dashboard.py
- Спробуйте інший порт: `DASHBOARD_PORT=8000`
- Перевірте firewall

---

## Наступні кроки

Перейдіть до [USAGE.md](USAGE.md) для вивчення всіх можливостей системи!

## Потрібна допомога?

Створіть issue на GitHub: https://github.com/kobzarbro-droid/smm-analise/issues
