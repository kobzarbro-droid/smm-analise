# Telegram Bot Module

Модуль Telegram бота для системи моніторингу Instagram SMM. Надсилає автоматичні звіти, сповіщення та статистику українською мовою.

## Структура модуля

```
src/telegram/
├── __init__.py          # Ініціалізація модуля
├── bot.py              # Основний клас Telegram бота
├── formatters.py       # Утиліти форматування повідомлень
└── reports.py          # Генератори звітів (щоденні, тижневі, місячні)
```

## Основні компоненти

### 1. TelegramBot (`bot.py`)

Основний клас для взаємодії з Telegram API.

**Можливості:**
- Надсилання повідомлень з HTML форматуванням
- Автоматичне розбиття довгих повідомлень
- Обробка команд користувача
- Планові звіти
- Сповіщення про події

**Приклад використання:**

```python
from src.telegram.bot import TelegramBot

# Ініціалізація бота
bot = TelegramBot()

# Надіслати щоденний звіт
await bot.send_daily_report()

# Надіслати тижневий звіт
await bot.send_weekly_report()

# Надіслати місячний звіт
await bot.send_monthly_report()

# Надіслати сповіщення
await bot.send_notification(
    title="Нова ціль досягнута!",
    message="Сьогодні опубліковано 3 пости",
    level="success"
)

# Надіслати попередження
await bot.send_alert(
    "Залученість нижче цільової (2.5%)",
    level="warning"
)
```

### 2. MessageFormatter (`formatters.py`)

Утиліти для форматування повідомлень з емоджі, прогрес-барами та українським текстом.

**Можливості:**
- Прогрес-бари (█████░░░░░ 50%)
- Форматування чисел (1.2K, 1.5M)
- Форматування дат українською
- Відображення змін зі стрілками (⬆️ +15%)
- HTML форматування для Telegram
- Велика колекція емоджі

**Приклад використання:**

```python
from src.telegram.formatters import MessageFormatter

formatter = MessageFormatter()

# Прогрес-бар
progress = formatter.progress_bar(6, 10)  # ██████░░░░

# Форматування числа
num = formatter.format_number(12345)  # 12.3K

# Форматування відсотка
pct = formatter.format_percentage(3.456)  # 3.46%

# Форматування дати
date = formatter.format_date(datetime.now(), 'long')  # 15 січня 2024

# Відображення зміни
change = formatter.format_change(150, 100)  # ⬆️ +50 (+50.0%)

# Прогрес до цілі
progress = formatter.format_target_progress(
    name="Пости",
    current=15,
    target=20,
    emoji="📷"
)

# Статистика залученості
stats = formatter.format_engagement_stats(
    likes=1234,
    comments=56,
    saves=78,
    engagement_rate=4.5
)

# Емоджі
emoji_chart = formatter.EMOJI['chart']  # 📊
emoji_heart = formatter.EMOJI['heart']  # ❤️
```

### 3. Report Classes (`reports.py`)

Три класи для генерації різних типів звітів.

#### DailyReport

Щоденний звіт з інформацією про:
- Кількість опублікованих постів та сторіс
- Статистика залученості
- Найкращий пост дня
- Рекомендації AI
- Попередження про невиконані цілі

```python
from src.telegram.reports import DailyReport

report = DailyReport()
report_text = await report.generate()  # За сьогодні
report_text = await report.generate(specific_date)  # За конкретну дату
```

#### WeeklyReport

Тижневий звіт з:
- Виконання цілей (прогрес-бари)
- Топ-3 пости тижня
- Порівняння з минулим тижнем
- Статистика залученості
- Рекомендації на наступний тиждень

```python
from src.telegram.reports import WeeklyReport

report = WeeklyReport()
report_text = await report.generate()  # За останній тиждень
```

#### MonthlyReport

Місячний звіт з:
- Загальна статистика (пости, сторіс, reels)
- Виконання місячних цілей
- Ріст аудиторії
- Топ-5 постів місяця
- Тренди та порівняння
- Рекомендації на наступний місяць
- Інсайти

```python
from src.telegram.reports import MonthlyReport

report = MonthlyReport()
report_text = await report.generate()  # За поточний місяць
report_text = await report.generate(specific_month)  # За конкретний місяць
```

## Команди бота

Користувачі можуть взаємодіяти з ботом через наступні команди:

- `/start` - Привітання та список команд
- `/help` - Детальна довідка
- `/daily` - Отримати щоденний звіт
- `/weekly` - Отримати тижневий звіт
- `/monthly` - Отримати місячний звіт
- `/stats` - Швидка статистика за останні 7 днів
- `/status` - Статус системи

## Налаштування

Необхідні змінні середовища в `config/.env`:

```env
# Telegram Bot
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_CHAT_ID=your_chat_id_here

# Графік звітів
DAILY_REPORT_TIME=20:00
WEEKLY_REPORT_DAY=0  # 0 = понеділок
WEEKLY_REPORT_TIME=09:00
MONTHLY_REPORT_TIME=09:00
```

## Інтеграція з планувальником

Для автоматичної відправки звітів використовуйте функції:

```python
from src.telegram.bot import (
    send_scheduled_daily_report,
    send_scheduled_weekly_report,
    send_scheduled_monthly_report
)

# У планувальнику (scheduler)
scheduler.add_job(
    send_scheduled_daily_report,
    trigger='cron',
    hour=20,
    minute=0
)

scheduler.add_job(
    send_scheduled_weekly_report,
    trigger='cron',
    day_of_week='mon',
    hour=9,
    minute=0
)

scheduler.add_job(
    send_scheduled_monthly_report,
    trigger='cron',
    day=1,
    hour=9,
    minute=0
)
```

## Запуск бота в режимі polling (для тестування)

```python
from src.telegram.bot import TelegramBot

bot = TelegramBot()
bot.run_polling()  # Блокуючий виклик
```

Або асинхронно:

```python
import asyncio
from src.telegram.bot import TelegramBot

async def main():
    bot = TelegramBot()
    await bot.start_polling()

asyncio.run(main())
```

## Особливості реалізації

### Асинхронність
Всі операції бота виконуються асинхронно з використанням `async/await`.

### Обробка помилок
Всі критичні операції обгорнуті в `try/except` блоки з логуванням помилок.

### Форматування повідомлень
Повідомлення використовують HTML форматування Telegram:
- `<b>текст</b>` - жирний
- `<i>текст</i>` - курсив
- `<code>текст</code>` - моноширинний шрифт

### Розбиття довгих повідомлень
Повідомлення автоматично розбиваються на частини, якщо перевищують ліміт Telegram (4096 символів).

### Українська локалізація
Всі тексти, назви місяців та днів тижня українською мовою.

## Тестування

Запустіть тести:

```bash
python -m unittest tests.test_telegram_bot -v
```

## Приклади звітів

### Щоденний звіт
```
═════════════════════════
📅 Звіт за 15 січня 2024 📅
═════════════════════════

📷 Опубліковано
📷 Пости: 2 ✅
🎬 Сторіс: 3 ✅

📊 Статистика залученості
❤️ Лайки: 1.2K
💬 Коментарі: 56
💾 Збереження: 78
📊 Залученість: 4.50%

🏆 Найкращий пост дня
...
```

### Тижневий звіт
```
═════════════════════════
📊 Тижневий звіт 📊
15.01.2024 - 21.01.2024
═════════════════════════

🎯 Виконання цілей
📷 Пости ✅
████████░░ 16/20 (80%)

🎬 Сторіс ✅
████████████ 21/21 (100%)
...
```

### Місячний звіт
```
═════════════════════════
📊 Місячний звіт 📊
Січень 2024
═════════════════════════

📖 Загальна статистика
📷 Пости: 22
🎬 Сторіс: 93
🔥 Reels: 9
...
```

## Підтримка

Для питань та проблем звертайтесь до документації проекту або створюйте issue на GitHub.
